#include "../include/cub3d.h"

/* 	do_moves(t_vars *vars, int key)
{
	if (key == 13)
	{

	}
	else if (key == 0)
	{

	}
	else if (key == 1)
	{

	}
	else if (key == 2)
	{

	}
}

int	key_hook(int key, t_vars *vars)
{
	if (key == 13 || key == 0 || key == 2 || key == 1)
		do_moves(vars, key);
	else if (key == 53)
	{
		printf("Ark, pourquoi tu fermes le jeu, il est trop cool\n");
		exit (0);
	}
	return (0);
} */